if ! command -v devenv >/dev/null 2>&1; then
  echo "devenv is required for this project." >&2
  echo "Install with: nix profile install nixpkgs#devenv" >&2
  exit 1
fi

eval "$(devenv direnvrc)"
use devenv
